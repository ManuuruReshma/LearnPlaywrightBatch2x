console.log(greeting);
var greeting = "Hello, World!";

// In the above code, the variable 'greeting' is hoisted to the top of its scope, but its value is not assigned until the line where it is declared. Therefore, when we try to log 'greeting' before its declaration, it will output 'undefined' instead of throwing an error.          
console.log(greeting);

