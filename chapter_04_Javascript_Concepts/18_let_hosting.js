console.log(greeting);
let greeting = "Hello, World!";

// In the above code, the variable 'greeting' is not hoisted to the top of its scope like 'var' variables. Therefore, when we try to log 'greeting' before its declaration, it will throw a ReferenceError instead of outputting 'undefined'.   

console.log(greeting);

{

}