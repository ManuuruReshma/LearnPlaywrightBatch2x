let unknown: unknown = "hello";

if (typeof unknown === "string") {
    console.log("Hi");
}

let message: string = "Hello world";
let username: string;
let userId: number;

//Function and every we need to type for attributes.

